//wait for the page to load
//select the table we want
//run the DataTables function on it

$(document).ready(function() {
    $('#example').DataTable();
} );

//DataTables adds ordering, searching, paging, and information to the table by default
